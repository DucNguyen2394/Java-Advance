package collection;

import java.util.Iterator;

public class Theory {
	// Đưa dữ liệu vào collection để an toàn.
	// hasNext() lặp đến khi nào hết thì thôi;
	// ArrayList xử lý luồng đơn;
	// đa luồng phải sử dụng Vector;
	// HashMap ko cho phép chứa giá trị trùng lặp;
	// LinkedList linh hoạt hơn;
	// TreeMap sử dụng cho danh sách theo kiểu phân cấp;
	// thiết lập độ ưu tiên thì dùng queue;
	
	Iterator it;
	// Iterator sử dụng kiểm tra phần tử trong collection;
}
